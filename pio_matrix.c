#include <stdio.h>
#include "pico/stdlib.h"
#include "hardware/pio.h"
#include "hardware/clocks.h"

#include "pio_matrix.pio.h" // Programa PIO para controle dos LEDs

int main()
{
  stdio_init_all(); // Inicializa a comunicação serial para depuração

  while (true)
  {
  }
}
